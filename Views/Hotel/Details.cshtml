@model GBC_Travel_Group_76.Models.Hotel

<h2>Hotel Details</h2>

<div>
    <h4>Hotel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Name:</dt>
        <dd>@Model.Name</dd>

        <dt>Location:</dt>
        <dd>@Model.Location</dd>

        <dt>Price Per Night:</dt>
        <dd>@Model.PricePerNight</dd>

        <dt>Rating:</dt>
        <dd>@Model.Rating</dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>
